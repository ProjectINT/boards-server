# Ф0: пробный эхо-сокет (временно, до фазы Ф1)

Сейчас в корне репозитория лежит не сервис, а пробник: `server.mjs`, `package.json` и `Dockerfile`
отвечают на один вопрос — **проходит ли WebSocket-апгрейд через маршрут dockhost по пути**
(риск №1 в `docs/PLAN_BOARD_SERVER_REPO_2026-09-10.md` §15 репозитория сайта).
Фаза Ф1 заменяет все три файла настоящим каркасом, `PROBE.md` и `check.mjs` тогда удаляются.

## Что делает пробник

- `GET /healthz` (и `/board-ws/healthz`) → `ok` — для HEALTHCHECK и проверки маршрута обычным `curl`;
- любой апгрейд → шлёт `hello <path>` и возвращает обратно всё присланное;
- в лог пишет `upgrade ok <path> <host> <x-forwarded-for>` — видно, дошёл ли апгрейд до контейнера
  и какой путь донёс маршрут.

Локально проверено 16.09.2026: `PORT=8099 node server.mjs` → `/healthz` = `ok`, кадр вернулся дословно.

## Развёртывание на dockhost

1. **Контейнер.** «Контейнеры» → «Добавить»: источник — репозиторий `ProjectINT/boards-server`,
   ветка `main`, корневой `Dockerfile`; открытый порт `8080/TCP`; ресурсы минимальные
   (0,1 CPU, 128 МБ — на время пробы; по §12 плана боевые 0,5 CPU / 512 МБ).
   Переменные окружения пробнику не нужны вообще, кроме необязательного `PORT`.
2. **Маршрут.** «Сетевые сервисы» → «Маршруты» → «Добавить»
   (`docs/DOCKHOST_ROUTES.md` в репозитории сайта):

   | Поле | Значение |
   |---|---|
   | Имя | `board-ws` |
   | Домен | `egesto.ru` |
   | Путь | `/board-ws` |
   | Тип | Внутренний |
   | Сетевой сервис | сервис этого контейнера |
   | Порт сетевого сервиса | `8080/TCP` |

## Проверка

```bash
curl -i https://egesto.ru/board-ws/healthz     # ждём 200 ok — маршрут до контейнера есть
npm install                                     # один раз, локально
node check.mjs wss://egesto.ru/board-ws
```

Успех — `АПГРЕЙД ПРОШЁЛ` и `ЭХО СОВПАЛО`; тогда адрес сервиса в плане остаётся `wss://egesto.ru/board-ws`.

Провал (`HTTP 404/426/502` или таймаут) — по §15 плана репозитория запасные варианты:
отдельный домен `board.egesto.ru` (тип «Внешний», A-запись на выданный IP — `docs/DOCKHOST_DOMAINS.md`)
с маршрутом на путь `/`, либо «Сетевой сервис / Порт» по образцу LiveKit. Перед запасным вариантом
стоит один раз перепроверить тот же контейнер на пути `/` — это разделит «шлюз не пропускает апгрейд»
и «не пропускает апгрейд именно на подпути».

Известно заранее: шлюз dockhost апгрейд по пути **умеет** — Supabase Realtime на домене
`…gw-1a.dockhost.net` по пути `/realtime/v1/websocket` отвечает `101` через `kong/2.8.1`.
Но там маршрут ведёт на kong, а не на свой сервис, поэтому проверка нужна всё равно.
